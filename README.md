# HelpMate-AI
HelpMate AI is building an internal assistant that uses an LLM to support customer service teams. The system can search the web, check order status, and calculate shipping costs. Based on the user’s query, the model selects the correct tool, and the application executes it to deliver accurate results.
